import React from 'react';
import ReactDOM from 'react-dom';
import './styles/App.css'; // Ensure this path matches your CSS location
import App from './App';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
